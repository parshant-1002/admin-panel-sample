// Define the type for the data parameter
export interface FormData {
  title: string;
  faqTagLabel: string;
  isVisible?: boolean;
  faqIsVisible?: boolean;
  faqTitle?: boolean;
}

export const HERO_SECTION_SUB_ENUM = {
  HERO_IMAGES: 'heroImages',
};
